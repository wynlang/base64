# base64 — Official Wyn Package

Base64 encode/decode. Uses built-in Encoding module.

## Usage

```wyn
var encoded = Encoding.base64_encode("Hello!")
var decoded = Encoding.base64_decode(encoded)
```
